# -Threads プロジェクト全体ルール

育児系アフィリエイトThreadsアカウント「ラクして楽しむパパ育児」(@papa_rakuiku)の運用プロジェクト。
2歳の娘を持つパパ目線のアカウント。詳細は [strategy/01_persona.md](strategy/01_persona.md) を参照。

このファイルはプロジェクト全体の共通ルールをまとめたもの。各エージェントの役割専用ルールは
`.claude/agents/<フォルダ名>/CLAUDE.md` にあるので、作業時は必ずそちらも合わせて読むこと
(フォルダ名とエージェント名の対応は下記「投稿パイプライン」の一覧を参照)。

## 投稿パイプライン(1日5投稿)

固定時間帯: **7:00 / 10:00 / 12:00 / 17:00 / 21:00**。**10:00と17:00はアフィリエイト枠固定**、他は共感・Tips・エッセイ系。

```
リサーチ担当(SNS優先でリサーチ)
      ↓
構成担当(構成案作成・投稿タイプで振り分け)
      ↓                          ↓
共感ライター(共感/Tips/エッセイ)   アフィリエイトライター(商品紹介)
      ↓                          ↓
              校正担当(校正・事実確認・論理チェック)
```

## 原稿の保存先【確定ルール】

**執筆後の下書きは`drafts/`に保存する。校正を経た原稿はユーザーに提示し、明示的な承認を得てから`outputs/`に保存する。`content/`は既存投稿・旧テンプレシステムの保管場所として維持し、今後の新規完成原稿の正式な保存先には使用しない。**

| フォルダ | 用途 |
|---|---|
| `drafts/` | 下書き・校正前の原稿(例: `drafts/YYYY-MM-DD-HHMM.md`) |
| `outputs/` | ユーザーが明示的に承認した完成原稿の正式な保存先(例: `outputs/YYYY-MM-DD.md`) |
| `content/` | 既存投稿・旧テンプレシステムの保管場所(**新規の完成原稿は保存しない**。既存ファイルは削除・移動・書き換えをしない) |

- 共感ライター/アフィリエイトライターが作成した本文は、校正担当に渡す前に`drafts/`配下に保存する
- 校正担当の校正後、**ユーザーの明示的な承認を得るまで`outputs/`には保存しない**(承認前の原稿は`drafts/`に置いたままにする)
- `drafts/legacy/`には、承認記録が確認できない過去の完成原稿のコピーを「承認状況:不明」と明記して置いている

5つの役割それぞれに専属エージェントを定義している(`.claude/agents/`配下)。各エージェントの定義ファイル
(`<エージェント名>.md`、日本語名)はフロントマター(`name`/`description`/`tools`/`model`)を保持したまま、
詳細ルールは対応するフォルダ内の`CLAUDE.md`に分離してある。**エージェント定義ファイル自体は移動・リネームしない**
(Agentツールの`subagent_type`として認識されなくなるため)。

フォルダ名は英数字にしてある(GitHubモバイルアプリで日本語名のフォルダ=ディレクトリを開こうとすると
「Error loading page」になる不具合の回避のため。エージェント定義ファイル名・`name`フロントマターは
日本語のままで、Agentツールでの呼び出しには影響しない)。対応関係:

| エージェント名(呼び出し名) | フォルダ名 |
|---|---|
| リサーチ担当 | `.claude/agents/research/` |
| 構成担当 | `.claude/agents/structure/` |
| 共感ライター | `.claude/agents/empathy-writer/` |
| アフィリエイトライター | `.claude/agents/affiliate-writer/` |
| 校正担当 | `.claude/agents/proofread/` |

## 全投稿共通の表記ルール

- ダブルクォーテーション(")は使わない。強調・引用は「」【】『』を使う
- 半角の`?`・`!`は使わない。全角の**？・！**を使う
- **「。」の後は必ず改行する**。1文=1パラグラフを基本にし、複数の文を1つの段落にまとめない(感情エッセイ型のポエム的な句読点前改行は例外)
- 「らしい」「みたい」など確信の持てないニュアンスの語は避ける。伝聞は「〜だって」を使う(特にアフィリエイト投稿で厳守)

## アフィリエイト投稿(10:00/17:00枠)の構成ルール

`[ペルソナ名指し+商品名+一言(バリエーション)]` → `[一番強いポイント1つ、言い切り]` → `[悩み・あるあるの具体エピソード1個]`

- 文字数200〜300字(改行除く)
- 一言バリエーションは投稿ごとに変える。「これ話題になってるらしいよ」は禁止
- 良いポイントは箇条書きにせず1つに絞り、ペルソナフックの直後(2番目)に置く
- 本文に「気になる人はリプ欄見てみて」等の明示的リンク誘導フレーズは書かない(誘導はリプ欄側で完結)
- リプ欄用案内文は「1行目:最後の訴求文/2行目:`[リンクを挿入]  pr`」固定(PR表記省略不可、ステマ規制対応)
- **商品を確定する前に「悩み→解決策→商品」のつながりが本当に成立しているか一段階ずつ確認する**。人気・レビュー数だけで選ばない。商品ページの「子供向け」記載がSEOキーワード欄の詰め込みでないかWebFetchで確認する

## 事実確認(ファクトチェック)は必須

校正担当が最終工程で以下を必ず確認し、投稿ごとに「根拠(ファクトチェック)」欄を残す。

- 発達・身体能力の描写が年齢相応か(WebSearchで確認)
- アフィリエイト商品の特徴(素材・機能等)が商品ページの実際の記載と一致しているか(WebFetchで確認)
- 悩みと商品のつながりが論理的に破綻していないか

### 過去の失敗事例(再発防止のため必ず確認する)

1. 「2歳児が全力疾走する」という不正確な発達描写 → 正しくは「よく転ぶ・小走り」
2. 商品ページと異なる素材の誤記(シリコン製と書いたが実際はポリエステル/TPU)
3. 商品ページのSEOキーワード欄に釣られて、実際は子供向けでない商品を「2歳児向け」として紹介(セタフィル→ピジョンベビークリームに訂正)
4. 悩みと無関係な商品を当てるロジック破綻(「日陰がない/エアコンがない→暑い」にレジャーシートを当てて破綻→首かけ扇風機に訂正)

## 使わないもの(参考資料として保持のみ)

- 旧初週ストック・アフィリエイト14枠分の試作(構成案・ドラフト・校正済み最終版)は、担当した各エージェントの
  フォルダ直下の`past-work/`フォルダ(例: `.claude/agents/research/past-work/`)に移動済み。
  **過去の試作であり実投稿には使わない**。型の妥当性チェックの参考程度に留める
- `content/`配下の既存ファイル(実投稿30件の書き起こしデータ・旧テンプレシステム・過去の日次ストック)は**保管場所として維持する**。削除・移動・書き換えをしない。**今後の新規完成原稿は`content/`ではなく`outputs/`に保存する**(上記「原稿の保存先【確定ルール】」を参照。作り方は[README.md](README.md)の「日次投稿ストックの作り方」を参照)

## 楽天API(未解決事項)

サーバーサイドからの商品検索API呼び出しは`403 REQUEST_CONTEXT_BODY_HTTP_REFERRER_MISSING`エラーで未解決(楽天サポートに問い合わせ中)。
詳細と暫定対応(WebSearch+WebFetchでの手動商品リサーチ)は [strategy/08_rakuten_api_setup.md](strategy/08_rakuten_api_setup.md)(コピー: [docs/setup/rakuten-api.md](docs/setup/rakuten-api.md))を参照。
アフィリエイトリンクは調査担当が具体的な商品名・商品ページURLを提示した後、**ユーザー本人が手動で取得する**。

## フォルダ整理(参照用コピー)について

`knowledge/`・`planning/`・`docs/setup/`配下のファイルは、`strategy/`配下の既存ファイルの**参照用コピー**です。
`strategy/`・`content/`の元ファイルは削除・変更しておらず、引き続き存在します。コピー元とコピー先の対応は
各コピー先ファイル末尾の「出典」に記載しています。両者に差異が生じた場合は、どちらかを優先して統合せず、
差異として報告してください。

`drafts/legacy/`には、承認記録が確認できない過去の完成原稿(`content/2026-09-18.md`・`2026-09-19.md`)を
「承認状況:不明」と明記した上でコピーしています。`outputs/`は、ユーザー承認が確認できた完成原稿の正式な保存先ですが、
承認済みの原稿がまだ無いため現時点では空です(詳細は上記「原稿の保存先【確定ルール】」)。
